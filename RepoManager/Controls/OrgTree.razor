@using Syncfusion.Blazor.Navigations


<h4>Sample Treeview</h4>
<p style="color: darkred; font-weight: bold;">Ensure view is updated to reflect design</p>
<SfTreeView TValue="RepoItem">
    <TreeViewFieldsSettings TValue="RepoItem" Id="Id" DataSource="@RepoList" Text="RepoName" HasChildren="HasSubFolders" Expanded="Expanded"></TreeViewFieldsSettings>
</SfTreeView>

@code{
    public class RepoItem
    {
        public string Id { get; set; } = "";
        public string ParentId { get; set; } = "";
        public string OrgName { get; set; } = "";
        public bool Expanded { get; set; }
        public bool HasSubFolders { get; set; }
    }
    List<RepoItem> RepoList = new List<RepoItem>();
    protected override void OnInitialized()
    {
        base.OnInitialized();
        RepoList.Add(new RepoItem
        {
            Id = "1",
            OrgName = "User's Github Username",
            HasSubFolders = true,
            Expanded = true
        });
        RepoList.Add(new RepoItem
        {
            Id = "2",
            ParentId = "1",
            OrgName = "Sample Organisation 1",
        });
         RepoList.Add(new RepoItem
        {
            Id = "2",
            ParentId = "1",
            OrgName = "Sample Organisation 2",
        });
         RepoList.Add(new RepoItem
        {
            Id = "2",
            ParentId = "1",
            OrgName = "Sample Organisation 3",
        });
        
    }
}